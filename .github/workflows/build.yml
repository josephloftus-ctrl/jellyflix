name: Build tvOS App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 30

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer

    - name: Show Xcode version
      run: xcodebuild -version

    - name: Resolve Swift packages
      run: |
        xcodebuild -resolvePackageDependencies \
          -project Stingray.xcodeproj \
          -scheme Stingray

    - name: Build for tvOS (Unsigned)
      run: |
        xcodebuild build \
          -project Stingray.xcodeproj \
          -scheme Stingray \
          -sdk appletvos \
          -configuration Release \
          -derivedDataPath build/DerivedData \
          -skipPackagePluginValidation \
          ARCHS=arm64 \
          ONLY_ACTIVE_ARCH=NO \
          SKIP_INSTALL=NO \
          BUILD_LIBRARY_FOR_DISTRIBUTION=NO \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          DEVELOPMENT_TEAM="" \
          PROVISIONING_PROFILE_SPECIFIER="" \
          2>&1 | tee build.log

    - name: Show build errors
      if: failure()
      run: |
        echo "=== BUILD ERRORS ==="
        grep -E "error:|warning:" build.log | head -50 || echo "No errors found in grep"
        echo "=== END ERRORS ==="

    - name: Debug build products
      run: |
        echo "=== Build Products ==="
        find build/DerivedData/Build/Products -type d -maxdepth 3
        echo "=== App contents ==="
        APP_PATH=$(find build/DerivedData/Build/Products -name "Stingray.app" -type d | head -1)
        if [ -n "$APP_PATH" ]; then
          ls -la "$APP_PATH/"
          ls -la "$APP_PATH/Stingray" 2>/dev/null || echo "No binary found"
          du -sh "$APP_PATH"
        fi

    - name: Create Unsigned IPA
      run: |
        APP_PATH=$(find build/DerivedData/Build/Products -name "Stingray.app" -type d | head -1)
        echo "Using app: $APP_PATH"
        if [ -z "$APP_PATH" ]; then
          echo "ERROR: No .app found"
          exit 1
        fi
        APP_SIZE=$(du -sm "$APP_PATH" | cut -f1)
        echo "App size: ${APP_SIZE}MB"
        if [ "$APP_SIZE" -lt 1 ]; then
          echo "ERROR: App bundle is suspiciously small (${APP_SIZE}MB)"
          exit 1
        fi
        mkdir -p build/Payload
        cp -r "$APP_PATH" build/Payload/
        cd build
        zip -r Jellyflix-unsigned.ipa Payload
        rm -rf Payload
        ls -lh Jellyflix-unsigned.ipa

    - name: Upload IPA Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Jellyflix-unsigned-ipa
        path: build/Jellyflix-unsigned.ipa
        retention-days: 30
